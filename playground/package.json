{
  "private": true,
  "name": "playground",
  "scripts": {
    "build": "nuxt build",
    "dev": "nuxt dev",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare"
  },
  "devDependencies": {
    "@nuxt/webpack-builder": "latest",
    "@open-editor/vite": "workspace:*",
    "@open-editor/webpack": "workspace:*",
    "nuxt": "^3.6.5"
  }
}
